---
phase: 05-today-briefing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - EDGE/Sources/DesignSystem/Components/CountdownPill.swift
  - EDGE/Sources/Features/Today/TodayView.swift
  - EDGE/Sources/Features/Matches/MatchDetailView.swift
autonomous: false
requirements: [TODAY-01, TODAY-02]
user_setup: []
must_haves:
  truths:
    - "Today shows a next-match spotlight with the pick ring, strength bar, tier + leverage pills"
    - "Tapping the spotlight pushes a match detail screen"
    - "The whole screen assembles top-to-bottom on open and replays on pull-to-refresh"
    - "Pull-to-refresh flips the status to 'Generating your briefing…'"
  artifacts:
    - path: "EDGE/Sources/DesignSystem/Components/CountdownPill.swift"
      provides: "frosted countdown capsule"
      contains: "Format.countdown"
    - path: "EDGE/Sources/Features/Today/TodayView.swift"
      provides: "spotlight + picks list + assembly/refresh wiring"
      contains: "navigationDestination"
    - path: "EDGE/Sources/Features/Matches/MatchDetailView.swift"
      provides: "match detail (stub; full version in Phase 6)"
      contains: "MatchDetailView"
  key_links:
    - from: "EDGE/Sources/Features/Today/TodayView.swift"
      to: "MatchDetailView"
      via: "navigationDestination(for: String.self) resolves match.id"
      pattern: "navigationDestination\\(for: String"
---

<objective>
Complete the Today briefing: the next-match spotlight, today's other picks, navigation to a match
detail (stub), and the self-assembly + pull-to-refresh replay.

Purpose: Finish the showcase home and prove the generative motion end-to-end (TODAY-01, TODAY-02).
Output: CountdownPill.swift, finished TodayView.swift, and a MatchDetailView.swift stub (Phase 6
replaces it with the full screen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/Core/Models.swift
@EDGE/Sources/Core/Format.swift
@EDGE/Sources/DesignSystem/Components/SoftRing.swift
@EDGE/Sources/DesignSystem/Components/StrengthBar.swift
@EDGE/Sources/DesignSystem/Components/SoftPill.swift
@EDGE/Sources/Features/Today/TodayView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: CountdownPill component</name>
  <files>EDGE/Sources/DesignSystem/Components/CountdownPill.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Format.swift</read_first>
  <action>
Create `CountdownPill.swift` (fills the spec's "CountdownPill" reference, which §5 left as inline):
a frosted `Capsule` (`.ultraThinMaterial` + `Theme.hairline`) containing a small `Theme.spark` dot
and `Text(Format.countdown(to: kickoff))` (`.calloutX`, `.monospacedDigit()`). Input: `kickoff: Date`.
Reused by Matches in Phase 6.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - CountdownPill.swift contains "Format.countdown" and "ultraThinMaterial" and "Theme.spark"
  </acceptance_criteria>
  <done>Countdown pill compiles</done>
</task>

<task type="auto">
  <name>Task 2: MatchDetailView stub</name>
  <files>EDGE/Sources/Features/Matches/MatchDetailView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift</read_first>
  <action>
Create a STUB `struct MatchDetailView: View { let match: Match … }` so Today's navigation compiles:
show the two teams (TeamBadge + name + "#rank"), the pick score in a `SoftRing`, the tier `SoftPill`,
and a `Text("Full detail in Phase 6")`. Title `"Matchday \(match.matchday)"`. Phase 6 (06-02)
REPLACES this file with the full detail per §7.4 — keep the same type name + initializer
`MatchDetailView(match:)`.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MatchDetailView.swift contains "struct MatchDetailView" and "let match: Match" and "init" compatible with MatchDetailView(match:)
  </acceptance_criteria>
  <done>Detail stub compiles and is pushable</done>
</task>

<task type="auto">
  <name>Task 3: Next-match spotlight + today's picks + navigation</name>
  <files>EDGE/Sources/Features/Today/TodayView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Today/TodayView.swift, EDGE/Sources/Core/Format.swift</read_first>
  <action>
Compute `upcoming = feed.matches.filter { $0.status == "upcoming" }.sorted { $0.kickoff < $1.kickoff }`
and `next = upcoming.first`. Append to the VStack (continuing the `.generativeAppear(i)` index):
- **Spotlight** (only if `next` exists): a `NavigationLink(value: next.id)` whose label is a `FrostCard`:
  `Eyebrow("Next up")` + `CountdownPill(kickoff: next.kickoff)` in an HStack; rows
  `TeamBadge(code: next.home.code)` + `next.home.name` and same for away; on the trailing side a
  `SoftRing(value: Format.ringFill(oneIn: next.myPick.exactChanceOneIn),
  color: Format.tierRingColor(next.myPick.tier)) { Text(next.myPick.score).font(.displayX) }`;
  a line `"Lands about \(Format.chanceText(oneIn: next.myPick.exactChanceOneIn)) — \(next.myPick.tierLabel.lowercased())."`;
  `StrengthBar(home: next.odds.home, draw: next.odds.draw, away: next.odds.away, homeCode: next.home.code, awayCode: next.away.code)`;
  an HStack of `SoftPill(tier)` (via `Format.tierStyle`) and `SoftPill(leverage)` (via `Format.leverageChip`);
  and a non-interactive **ink-pill label** "Why this pick →" (Capsule fill `Theme.ink`, white text +
  white circle chevron — matching InkButton's look but as a label inside the link).
- **Today's other picks**: `Eyebrow("Today's picks")` then a `VStack` over `upcoming.dropFirst()` —
  each a `NavigationLink(value: m.id)` row: `TeamBadge(m.home.code)`, `Text(m.myPick.score)`,
  `TeamBadge(m.away.code)`, spacer, a small tier dot `Circle().fill(Format.tierRingColor(m.myPick.tier))`.
Add ONE destination on the ScrollView/NavigationStack content:
`.navigationDestination(for: String.self) { id in if let m = feed.matches.first(where: { $0.id == id })
{ MatchDetailView(match: m) } }`. (Use `match.id` strings — do not require Match: Hashable.)
Empty state: if `upcoming.isEmpty`, show a FrostCard "No matches today — next up soon" with a soft
`calendar` glyph.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TodayView.swift contains "NavigationLink(value: next.id" (or m.id), "SoftRing(", "StrengthBar(", "navigationDestination(for: String.self)"
    - contains the ink-pill "Why this pick" label using Theme.ink
    - handles upcoming.isEmpty empty state
  </acceptance_criteria>
  <done>Spotlight + picks list render and push detail</done>
</task>

<task type="auto">
  <name>Task 4: Refresh replays assembly + status flips to "Generating…"</name>
  <files>EDGE/Sources/Features/Today/TodayView.swift</files>
  <read_first>EDGE/Sources/Features/Today/TodayView.swift, EDGE/Sources/Core/AppStore.swift</read_first>
  <action>
Add `@State private var generating = false`. Change `.refreshable` to:
`{ generating = true; await store.refresh(); generating = false }`, and pass `generating` into
`GeneratedStatus(updatedAt: feed.updatedAt, generating: generating)`. Because the VStack is keyed
`.id(store.generation)` (from 05-01) and `refresh()` bumps `generation`, the whole screen REMOUNTS
and the `.generativeAppear` stagger replays automatically. Verify the content `.id(store.generation)`
is still present.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TodayView.swift contains "generating = true" and "generating: generating" and ".id(store.generation)"
  </acceptance_criteria>
  <done>Pull-to-refresh replays the assembly and shows the generating status</done>
</task>

<task type="auto">
  <name>Task 5: Launch Today on simulator</name>
  <files></files>
  <action>Build/install/launch on a booted iOS 18 simulator (as in 01-02 Task 4); land on the Today tab.</action>
  <verify>xcrun simctl list devices | grep -i booted</verify>
  <done>Today running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Today briefing — the showcase home that assembles itself.</what-built>
  <how-to-verify>On launch, confirm the screen **builds itself top-to-bottom**: status → date → "Good <time>, Niko." → standing card (#8 of 9, "6 points behind Alex", gap rail marker sliding to ~75%) → tonight's-plan card → next-match spotlight (Portugal vs Congo DR, ring drawing to ~1/7 with "1:0", strength bar filling, "Coin-flip" + "Against the room" pills, black "Why this pick →" pill) → today's other picks. Each element should **blur-into-focus and rise**, staggered. Then **pull down to refresh**: the status flips to "Generating your briefing…" and the whole screen **re-assembles**. Tap the spotlight → a detail screen pushes (stub for now). It must feel light, opalescent, and alive.</how-to-verify>
  <resume-signal>Type "approved" or describe what's off (timing, blur, layout, copy)</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] Today assembles on open; refresh replays + shows "Generating…" (human-verified)
- [ ] Spotlight pushes detail (human-verified)
</verification>

<success_criteria>
- All tasks completed
- Today briefing complete and self-assembling with refresh replay (TODAY-01, TODAY-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-today-briefing/05-02-SUMMARY.md`
</output>
