---
phase: 08-motion-a11y-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/Features/Today/TodayView.swift
  - EDGE/Sources/Features/Matches/MatchesView.swift
  - EDGE/Sources/Features/Matches/MatchRowCard.swift
  - EDGE/Sources/Features/Matches/MatchDetailView.swift
autonomous: true
requirements: [MOTION-01]
user_setup: []
must_haves:
  truths:
    - "Tapping a match runs a zoom transition that morphs the card/ring into the detail"
    - "Opening a 'Spot on' result fires a success haptic"
    - "The greeting builds in word-by-word"
  artifacts:
    - path: "EDGE/Sources/Features/Matches/MatchDetailView.swift"
      provides: "zoom-transition destination + success haptic"
      contains: "navigationTransition"
  key_links:
    - from: "MatchRowCard / Today spotlight"
      to: "MatchDetailView"
      via: "matchedTransitionSource(id:) ↔ navigationTransition(.zoom(sourceID:))"
      pattern: "matchedTransitionSource"
---

<objective>
Finish the motion layer: the match→detail zoom morph (iOS 18), a success haptic on great results,
and the word-by-word greeting build.

Purpose: Land the last signature animations (MOTION-01). Rings/bars/stagger already animate from
earlier phases; this plan adds the cross-screen morph and finishing haptics.
Output: zoom transition wired source↔destination; success haptic; word-build greeting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/Features/Matches/MatchDetailView.swift
@EDGE/Sources/Features/Today/TodayView.swift
@EDGE/Sources/Features/Matches/MatchesView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zoom transition (card → detail morph)</name>
  <files>EDGE/Sources/Features/Matches/MatchesView.swift, EDGE/Sources/Features/Today/TodayView.swift, EDGE/Sources/Features/Matches/MatchDetailView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Matches/MatchesView.swift, EDGE/Sources/Features/Matches/MatchDetailView.swift</read_first>
  <action>
Use the iOS 18 zoom navigation transition (the reliable way to morph across a NavigationStack push;
matchedGeometryEffect does NOT animate across pushes — do not use it here):
- In `MatchesView` and `TodayView`: add `@Namespace private var ns`. On each source
  `NavigationLink(value: match.id) { … }`, attach `.matchedTransitionSource(id: match.id, in: ns)`.
- Pass the namespace into the destination: change the destination closure to
  `MatchDetailView(match: m, ns: ns)`.
- In `MatchDetailView`: add `var ns: Namespace.ID? = nil`. When `ns != nil`, attach
  `.navigationTransition(.zoom(sourceID: match.id, in: ns!))` to the root view.
The result: tapping a match card/spotlight zooms/morphs it into the detail (and back). Keep the
detail working when `ns == nil` (e.g. previews).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MatchesView.swift and TodayView.swift contain "@Namespace" and ".matchedTransitionSource(id:"
    - MatchDetailView.swift contains "var ns: Namespace.ID?" and ".navigationTransition(.zoom(sourceID:"
  </acceptance_criteria>
  <done>Card→detail zoom morph works both ways</done>
</task>

<task type="auto">
  <name>Task 2: Success haptic on a "Spot on" result</name>
  <files>EDGE/Sources/Features/Matches/MatchDetailView.swift</files>
  <read_first>EDGE/Sources/Features/Matches/MatchDetailView.swift</read_first>
  <action>
In `MatchDetailView`, add `.onAppear` that, if `match.myPick.result?.outcome == "exact"`, fires
`UINotificationFeedbackGenerator().notificationOccurred(.success)`. Gate it so it runs once.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MatchDetailView.swift contains "UINotificationFeedbackGenerator" and 'outcome == "exact"'
  </acceptance_criteria>
  <done>Opening an exact-score result feels rewarding</done>
</task>

<task type="auto">
  <name>Task 3: Word-by-word greeting build</name>
  <files>EDGE/Sources/Features/Today/TodayView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Today/TodayView.swift</read_first>
  <action>
Per §8.4: render the greeting as words — split `Format.greeting(feed.me.name)` on spaces into a
wrapping `HStack`/`FlowLayout` (or a simple `HStack` since it's short) where each word gets its own
`.generativeAppear(1 + wordIndex)` so it assembles word-by-word. Shift the indices of the cards below
so the overall top-to-bottom stagger still reads in order. Under Reduce Motion this naturally snaps
(GenerativeAppear already handles it).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TodayView.swift splits the greeting into words and applies generativeAppear per word
  </acceptance_criteria>
  <done>Greeting builds in word-by-word</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms zoom transition source+destination, success haptic, word-build greeting
</verification>

<success_criteria>
- All tasks completed
- Cross-screen morph + finishing haptics + greeting build complete (MOTION-01)
</success_criteria>

<output>
After completion, create `.planning/phases/08-motion-a11y-states/08-01-SUMMARY.md`
</output>
