---
phase: 08-motion-a11y-states
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - EDGE/Sources/Features/Shell/RootView.swift
  - EDGE/Sources/DesignSystem/Components/SoftRing.swift
  - EDGE/Sources/DesignSystem/Components/StrengthBar.swift
  - EDGE/Sources/DesignSystem/Components/IridescentGlow.swift
  - EDGE/Sources/Features/Matches/MatchDetailView.swift
  - EDGE/Sources/Features/Table/TableView.swift
autonomous: false
requirements: [A11Y-01, A11Y-02, LANG-01]
user_setup: []
must_haves:
  truths:
    - "Reduce Motion snaps all animation to final and pauses the gradient"
    - "VoiceOver reads worded labels for rings, strength bars, and table rows"
    - "Loading, offline-fallback, and hard-error states render gracefully"
    - "No probability/percentage/jargon text appears anywhere in the app"
  artifacts:
    - path: "EDGE/Sources/Features/Shell/RootView.swift"
      provides: "loading + error states"
      contains: "ContentUnavailableView"
  key_links:
    - from: "EDGE/Sources/Features/Shell/RootView.swift"
      to: "AppStore.phase"
      via: "renders loading/failed states"
      pattern: "case .loading|case .failed"
---

<objective>
Harden accessibility, add the app-level states, and prove the no-jargon rule across the whole app —
then run the full acceptance pass.

Purpose: Make EDGE robust and inclusive, and verify it meets every acceptance criterion (A11Y-01,
A11Y-02, LANG-01; plus a final sweep of all earlier requirements).
Output: VoiceOver labels, Reduce-Motion confirmation, loading/error states, a passing no-jargon
audit, and a human-approved acceptance run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/ios_app_plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceOver labels (worded, not numeric)</name>
  <files>EDGE/Sources/DesignSystem/Components/SoftRing.swift, EDGE/Sources/DesignSystem/Components/StrengthBar.swift, EDGE/Sources/DesignSystem/Components/IridescentGlow.swift, EDGE/Sources/Features/Matches/MatchDetailView.swift, EDGE/Sources/Features/Table/TableView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Matches/MatchDetailView.swift, EDGE/Sources/Features/Table/TableView.swift</read_first>
  <action>
Per §9: add an optional `var a11yLabel: String? = nil` to `SoftRing` and `StrengthBar`; when set,
apply `.accessibilityElement(children: .ignore).accessibilityLabel(a11yLabel!)`. Mark `IridescentGlow`
`.accessibilityHidden(true)` (decorative). At call sites:
- Detail hero ring: a11yLabel = `"Pick \(match.myPick.score), \(match.myPick.tierLabel), lands about
  \(Format.chanceText(oneIn: match.myPick.exactChanceOneIn))."`
- Strength bars: a11yLabel = `"\(Format.strengthWords(match.favoriteStrength)): \(match.favorite)."`
- Table rows: wrap each row `.accessibilityElement(children: .ignore).accessibilityLabel("#\(row.rank)
  \(row.name), \(row.points) points\(row.isMe ? \", you\" : \"\")\(row.isLeader ? \", leader\" : \"\").")`.
Keep labels in WORDS — never read a raw probability.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - SoftRing.swift and StrengthBar.swift accept an a11yLabel and apply accessibilityLabel
    - IridescentGlow.swift contains "accessibilityHidden(true)"
    - TableView.swift adds an accessibilityLabel per row
  </acceptance_criteria>
  <done>Screen reader hears worded descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Loading + offline + hard-error states</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Shell/RootView.swift, EDGE/Sources/Core/AppStore.swift</read_first>
  <action>
Per §10, handle `store.phase` at the RootView level (without breaking the tabbed UI when data exists):
- If `store.feed != nil` → show the normal TabView (data present always wins; offline never blocks).
- Else if `case .loading` → a centered soft skeleton (a pulsing `SoftRing`-style placeholder) over
  `Theme.bg` + `IridescentGlow`.
- Else if `case .failed` → a light `ContentUnavailableView` titled "Couldn't load your league" with an
  `InkButton("Retry") { Task { await store.load() } }`.
(Since the bundled feed always decodes, the failed branch is a safety net.) Optionally, if a future
remote load failed but bundled data exists, show a tiny "offline — showing last saved" `SoftPill`
under the Today `GeneratedStatus` (leave a TODO comment if remote isn't wired in v1).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "case .loading", "case .failed", "ContentUnavailableView", "Retry"
    - data-present path still renders the TabView
  </acceptance_criteria>
  <done>Graceful loading/error states</done>
</task>

<task type="auto">
  <name>Task 3: No-jargon audit (prove LANG-01)</name>
  <files></files>
  <action>
Prove no forbidden content reaches the screen. Run these greps over `EDGE/Sources` and confirm the
results are empty or are non-UI (math only, never inside a `Text(...)`):
- `grep -rn "composite" EDGE/Sources` → expect NONE
- `grep -rn "probability\|Probability" EDGE/Sources` → expect NONE
- `grep -rnE "Text\([^)]*%" EDGE/Sources` → expect NONE (no percent signs rendered as text)
- `grep -rn "expectedPoints" EDGE/Sources` → ONLY in alternate-bar WIDTH math, NEVER inside `Text(...)`
- `grep -rnE "Text\([^)]*odds|Text\([^)]*\.home\b" EDGE/Sources` → expect NONE (no raw odds as text)
Read each hit and confirm it is not displayed as text. If any raw number/jargon is rendered, replace
it with a word/ring/bar/pill per §6 before proceeding.
  </action>
  <verify>grep -rn "composite\|probability" EDGE/Sources || echo "CLEAN: no jargon tokens"; grep -rnE "Text\([^)]*%" EDGE/Sources || echo "CLEAN: no percent text"</verify>
  <acceptance_criteria>
    - No "composite" or "probability" anywhere in EDGE/Sources
    - No `Text(...)` containing a literal "%" 
    - Every "expectedPoints" use is bar-width math, not Text
  </acceptance_criteria>
  <done>LANG-01 proven across the codebase</done>
</task>

<task type="auto">
  <name>Task 4: Build, launch, and self-check acceptance</name>
  <files></files>
  <action>
Build/install/launch on a booted iOS 18 simulator. Walk every screen and tick the §15 acceptance
list in docs/ios_app_plan.md: Today assembles + refresh replays; no jargon; picks show score+tier+
reasons; results show actual score + outcome pill; table highlights you + crowns leader; insights
form + scouting; rings draw / bars fill / cards stagger; tab + press haptics. Then enable Settings →
Accessibility → Reduce Motion and relaunch; confirm everything snaps and the gradient is static.
Fix any gap before the checkpoint.
  </action>
  <verify>cd EDGE && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3; xcrun simctl list devices | grep -i booted</verify>
  <done>All §15 criteria self-verified in the running app</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>EDGE v1 — the complete app: foundation, data, components, shell, Today, Matches+Detail, Insights+Scouting, motion, a11y, states.</what-built>
  <how-to-verify>Final acceptance — confirm each: (1) **Today assembles itself** on open and **pull-to-refresh replays** it; (2) the **gradient breathes** and the look is light/opalescent everywhere; (3) tapping a match **zoom-morphs** into a detail with **3 plain-English reasons** and **zero jargon** (no %, no probabilities, no raw 1.87); (4) **Results** show the real score + "Spot on +4"/"Missed" pills; (5) **Table** highlights you + crowns Alex + movement arrows, and a row tap opens a **scouting** card; (6) **Insights** shows form + a scouting card per friend; (7) turn on **Reduce Motion** → all animation snaps to final and the gradient is static; (8) **VoiceOver** on a ring/row reads a worded description. If all pass, EDGE v1 is done.</how-to-verify>
  <resume-signal>Type "approved" to complete the milestone, or list anything that fails</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] No-jargon greps clean (LANG-01)
- [ ] Reduce Motion snaps + pauses gradient; VoiceOver reads worded labels (human-verified)
- [ ] Full §15 acceptance checklist passes (human-verified)
</verification>

<success_criteria>
- All tasks completed
- A11Y-01, A11Y-02, LANG-01 satisfied; full app acceptance approved
- EDGE v1 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-motion-a11y-states/08-02-SUMMARY.md`
</output>
