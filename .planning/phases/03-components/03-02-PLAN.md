---
phase: 03-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/DesignSystem/Components/SoftPill.swift
  - EDGE/Sources/DesignSystem/Components/InkButton.swift
  - EDGE/Sources/DesignSystem/Components/MovementArrow.swift
  - EDGE/Sources/DesignSystem/Components/TeamBadge.swift
autonomous: true
requirements: [COMP-02]
user_setup: []
must_haves:
  truths:
    - "SoftPill renders a pastel chip with darker same-hue text"
    - "InkButton is a full-width black pill with a white circular icon and press-scale feedback"
    - "MovementArrow shows green-up / red-down / gray-none with the delta"
    - "TeamBadge is an asset-free frosted circular monogram"
  artifacts:
    - path: "EDGE/Sources/DesignSystem/Components/SoftPill.swift"
      provides: "state chip"
      contains: "struct SoftPill"
    - path: "EDGE/Sources/DesignSystem/Components/InkButton.swift"
      provides: "black primary pill + PressScale style"
      contains: "PressScale"
    - path: "EDGE/Sources/DesignSystem/Components/MovementArrow.swift"
      provides: "rank movement indicator"
      contains: "arrow.up.right"
    - path: "EDGE/Sources/DesignSystem/Components/TeamBadge.swift"
      provides: "circular monogram badge"
      contains: "struct TeamBadge"
---

<objective>
Build the chip + action + indicator components: SoftPill, InkButton (+PressScale), MovementArrow,
TeamBadge.

Purpose: The state pills, the single black CTA, the movement arrows, and team monograms recur across
every screen (COMP-02).
Output: Four component files, building clean. Runs in parallel with 03-01 (no shared files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: SoftPill</name>
  <files>EDGE/Sources/DesignSystem/Components/SoftPill.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `SoftPill.swift` with the EXACT `struct SoftPill` from §5.9: optional `systemImage`, text in
`.eyebrowX`, foreground `fg`, background `Capsule().fill(bg)`, padding h9/v5. Defaults `bg = Theme.actBG`,
`fg = Theme.actText`. The darker-text-on-pastel pairing (e.g. posText on posBG) is mandatory for
contrast — never plain gray/black on a colored fill.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - SoftPill.swift contains "Capsule().fill(bg)" and "Theme.actBG" and "Theme.actText"
  </acceptance_criteria>
  <done>Pill compiles with pastel fill + darker text</done>
</task>

<task type="auto">
  <name>Task 2: InkButton + PressScale</name>
  <files>EDGE/Sources/DesignSystem/Components/InkButton.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `InkButton.swift` with the EXACT `struct InkButton` and `struct PressScale: ButtonStyle` from
§5.10: a full-width `Capsule().fill(Theme.ink)` with white title (`.headlineX`) and a white circle
holding the (default `arrow.right`) symbol in `Theme.ink`; uses `.buttonStyle(PressScale())`.
PressScale scales to 0.97 on press with `.spring(response:0.3, dampingFraction:0.7)`. This is the
ONLY black/high-contrast control in the app — keep it rare.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - InkButton.swift contains "Capsule().fill(Theme.ink)", "struct PressScale: ButtonStyle", "scaleEffect(configuration.isPressed ? 0.97 : 1)"
  </acceptance_criteria>
  <done>Black pill + reusable press-scale style compile</done>
</task>

<task type="auto">
  <name>Task 3: MovementArrow</name>
  <files>EDGE/Sources/DesignSystem/Components/MovementArrow.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `MovementArrow.swift` with the EXACT `struct MovementArrow` from §5.11: `move > 0` →
`arrow.up.right` in `Theme.posText`; `< 0` → `arrow.down.right` in `Theme.negText`; `== 0` → `minus`
in `Theme.textMute`. Show `abs(move)` (`.eyebrowX`, `.monospacedDigit()`) when nonzero. Color is
paired with the number/icon (never color-only) per A11Y.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MovementArrow.swift contains "arrow.up.right", "arrow.down.right", "Theme.posText", "Theme.negText"
  </acceptance_criteria>
  <done>Movement indicator compiles</done>
</task>

<task type="auto">
  <name>Task 4: TeamBadge</name>
  <files>EDGE/Sources/DesignSystem/Components/TeamBadge.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `TeamBadge.swift` with the EXACT `struct TeamBadge` from §5.12: a 3-letter `code` in a frosted
`Circle().fill(.ultraThinMaterial)` + `Theme.card` overlay + `Theme.hairline` stroke. Default size 42,
font `size * 0.28` weight `.semibold`. Asset-free (no image pipeline).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TeamBadge.swift contains "ultraThinMaterial" and "Theme.hairline" and a size parameter
  </acceptance_criteria>
  <done>Team monogram badge compiles</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms each component matches the spec tokens/symbols
</verification>

<success_criteria>
- All tasks completed
- SoftPill/InkButton/MovementArrow/TeamBadge render per spec (COMP-02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-components/03-02-SUMMARY.md`
</output>
