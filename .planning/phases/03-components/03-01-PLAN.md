---
phase: 03-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/DesignSystem/Components/FrostCard.swift
  - EDGE/Sources/DesignSystem/Components/SoftRing.swift
  - EDGE/Sources/DesignSystem/Components/StrengthBar.swift
  - EDGE/Sources/DesignSystem/Components/GapRail.swift
autonomous: true
requirements: [COMP-01]
user_setup: []
must_haves:
  truths:
    - "FrostCard renders translucent frosted glass with a white hairline and soft shadow"
    - "SoftRing draws its arc from 0 to value on appear"
    - "StrengthBar segments and GapRail marker animate in on appear"
  artifacts:
    - path: "EDGE/Sources/DesignSystem/Components/FrostCard.swift"
      provides: "frosted glass container"
      contains: "ultraThinMaterial"
    - path: "EDGE/Sources/DesignSystem/Components/SoftRing.swift"
      provides: "drawing ring with custom center"
      contains: "trim(from: 0, to: animated)"
    - path: "EDGE/Sources/DesignSystem/Components/StrengthBar.swift"
      provides: "pastel 1X2 bar"
      contains: "StrengthBar"
    - path: "EDGE/Sources/DesignSystem/Components/GapRail.swift"
      provides: "you→leader timeline rail"
      contains: "GapRail"
---

<objective>
Build the four structural data-viz components: FrostCard, SoftRing, StrengthBar, GapRail.

Purpose: The frosted container + the animated rings/bars/rail are the load-bearing visual language
of every screen (COMP-01).
Output: Four component files, each animating per spec, building clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: FrostCard</name>
  <files>EDGE/Sources/DesignSystem/Components/FrostCard.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `FrostCard.swift` with the EXACT `struct FrostCard<Content: View>` from §5.5: a
`.ultraThinMaterial` background + `Theme.card` fill overlay + `RoundedRectangle(... rLg)` with a 1pt
`Theme.hairline` strokeBorder + `.shadow(color: Theme.shadow.opacity(0.10), radius: 24, y: 14)`.
Default padding `Theme.s4`, radius `Theme.rLg`. `@ViewBuilder var content`.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - FrostCard.swift contains "ultraThinMaterial", "Theme.hairline", "Theme.shadow.opacity(0.10)"
  </acceptance_criteria>
  <done>Frosted card compiles</done>
</task>

<task type="auto">
  <name>Task 2: SoftRing (draws on appear)</name>
  <files>EDGE/Sources/DesignSystem/Components/SoftRing.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `SoftRing.swift` with the EXACT `struct SoftRing<Center: View>` from §5.6: a `Theme.track`
track circle + a colored `Circle().trim(from: 0, to: animated)` arc with round line caps, rotated
-90°, a SOFT `.shadow(color: color.opacity(0.25), radius: 6)` (not neon), and a `@ViewBuilder center`.
On appear, animate `animated` 0→clamped value with `.spring(response: 1.0, dampingFraction: 0.9)
.delay(0.25)`; under `accessibilityReduceMotion` set it immediately. Defaults: lineWidth 8, size 108.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - SoftRing.swift contains "trim(from: 0, to: animated)", "accessibilityReduceMotion", "dampingFraction: 0.9"
    - shadow uses color.opacity(0.25) (soft, not a hard glow)
  </acceptance_criteria>
  <done>Ring draws its arc on appear, Reduce-Motion-safe</done>
</task>

<task type="auto">
  <name>Task 3: StrengthBar (pastel 1X2)</name>
  <files>EDGE/Sources/DesignSystem/Components/StrengthBar.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `StrengthBar.swift` with the EXACT `struct StrengthBar` from §5.7: a `GeometryReader` HStack
of three capsule segments — home `Color(hex:"9DBBA9")` (soft sage), draw `Theme.textMute`, away
`Color(hex:"A9C2E6")` (soft blue) — whose widths animate from 0 on appear via
`.easeOut(duration: 0.8).delay(0.2)` (snap under Reduce Motion). Below: eyebrow labels
`homeCode / "draw" / awayCode`. Height 8.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - StrengthBar.swift contains 'Color(hex: "9DBBA9")' and 'Color(hex: "A9C2E6")' and "accessibilityReduceMotion"
  </acceptance_criteria>
  <done>Pastel strength bar fills on appear</done>
</task>

<task type="auto">
  <name>Task 4: GapRail (you→leader timeline)</name>
  <files>EDGE/Sources/DesignSystem/Components/GapRail.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `GapRail.swift` with the EXACT `struct GapRail` from §5.8: a `Theme.track` rail, an iridescent
fill (`LinearGradient([Theme.iridBlue, Theme.iridLilac])`) to fraction `mine/leader`, and a white
marker circle (stroked `Theme.spark`) that slides to that fraction with
`.spring(response: 1.0, dampingFraction: 0.9).delay(0.3)` (snap under Reduce Motion). Add a
`leaderName: String` parameter and show "<mine> you" / "<leader> <leaderName>" beneath
(`.monospacedDigit()`). (The spec snippet has a placeholder "leader" label — replace it with the
passed `leaderName`.)
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - GapRail.swift contains "Theme.iridBlue", "Theme.spark", "accessibilityReduceMotion"
    - exposes a leaderName parameter (contains "leaderName")
  </acceptance_criteria>
  <done>Gap rail fills + marker slides on appear</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms each component's animation + Reduce-Motion gate
</verification>

<success_criteria>
- All tasks completed
- FrostCard/SoftRing/StrengthBar/GapRail render + animate per spec (COMP-01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-components/03-01-SUMMARY.md`
</output>
