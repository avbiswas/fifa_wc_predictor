---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - EDGE/Sources/DesignSystem/Components/IridescentGlow.swift
  - EDGE/Sources/DesignSystem/Components/GenerativeAppear.swift
  - EDGE/Sources/EDGEApp.swift
autonomous: false
requirements: [FND-03, FND-04]
user_setup: []
must_haves:
  truths:
    - "The iridescent gradient is visible and continuously breathing on launch"
    - "A placeholder element enters with a blur-to-focus + rise + scale animation"
    - "Both effects respect Reduce Motion (snap/pause)"
  artifacts:
    - path: "EDGE/Sources/DesignSystem/Components/IridescentGlow.swift"
      provides: "breathing pastel gradient driven by TimelineView"
      contains: "TimelineView"
    - path: "EDGE/Sources/DesignSystem/Components/GenerativeAppear.swift"
      provides: "blur-to-focus staggered entrance modifier"
      contains: "generativeAppear"
  key_links:
    - from: "EDGE/Sources/EDGEApp.swift"
      to: "IridescentGlow + generativeAppear"
      via: "root composes both"
      pattern: "IridescentGlow\\(|generativeAppear\\("
---

<objective>
Build the two signature aesthetic primitives — the breathing iridescent gradient and the generative
blur-to-focus entrance — and wire them into the app root.

Purpose: Prove the look-and-feel that defines the whole product before any screen exists (FND-03,
FND-04). These are the highest-priority motion pieces per the spec.
Output: IridescentGlow.swift, GenerativeAppear.swift, and an updated EDGEApp root that shows the
gradient with "EDGE" materializing into focus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: IridescentGlow (breathing pastel gradient)</name>
  <files>EDGE/Sources/DesignSystem/Components/IridescentGlow.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `IridescentGlow.swift` with the EXACT `struct IridescentGlow` from `docs/ios_app_plan.md`
§5.3 — five `Theme.irid*` blobs positioned by `baseX/baseY`, animated by `TimelineView(.animation(
minimumInterval: 1.0/30.0, paused: reduceMotion))` using `sin/cos(t * sx|sy)` for drift and scale,
wrapped in `.blur(radius: 44)` at `opacity(0.85 * intensity)`, with `.allowsHitTesting(false)`.
Keep the `@Environment(\.accessibilityReduceMotion)` gate exactly (paused TimelineView when on).
Expose `var intensity: Double = 1.0`. Do not substitute MeshGradient here — the blurred-blob version
is canonical (the spec notes MeshGradient only as an iOS-18 alternative).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - IridescentGlow.swift contains "TimelineView" and "accessibilityReduceMotion" and "blur(radius: 44)"
    - References Theme.iridBlue, iridLilac, iridBlush, iridPeach, iridMint
    - contains "allowsHitTesting(false)"
  </acceptance_criteria>
  <done>Gradient component compiles and is gated on Reduce Motion</done>
</task>

<task type="auto">
  <name>Task 2: GenerativeAppear modifier (blur-to-focus stagger)</name>
  <files>EDGE/Sources/DesignSystem/Components/GenerativeAppear.swift</files>
  <read_first>docs/ios_app_plan.md</read_first>
  <action>
Create `GenerativeAppear.swift` with the EXACT `struct GenerativeAppear: ViewModifier` and the
`extension View { func generativeAppear(_:) }` from §5.4. The entrance state MUST be: `opacity 0→1`,
`blur 9→0`, `scaleEffect 0.97→1`, `offset(y) 16→0`, animated with
`.spring(response: 0.75, dampingFraction: 0.82).delay(base + Double(index)*step)` where base=0.12,
step=0.085. Under Reduce Motion, set `shown = true` immediately (no animation). The blur-to-focus is
the defining texture — do not drop the `.blur`.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - GenerativeAppear.swift contains "blur(radius: shown ? 0 : 9)" and "scaleEffect(shown ? 1 : 0.97"
    - contains "response: 0.75, dampingFraction: 0.82"
    - extension exposes func generativeAppear(_ index: Int)
  </acceptance_criteria>
  <done>Modifier compiles; any view can stagger-enter with blur-to-focus</done>
</task>

<task type="auto">
  <name>Task 3: Wire gradient + generative entrance into the root</name>
  <files>EDGE/Sources/EDGEApp.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/EDGEApp.swift</read_first>
  <action>
Replace the temporary body so the root is:
`ZStack { Theme.bg.ignoresSafeArea(); IridescentGlow().ignoresSafeArea();
VStack(spacing: 8) { Text("EDGE").font(.heroLight); Text("agentic gen UI").font(.calloutX)
.foregroundStyle(Theme.textDim) }.generativeAppear(0) }` with `.preferredColorScheme(.light)`.
Keep `@main`. This is the visual proof screen for Phase 1.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - EDGEApp.swift contains "IridescentGlow(" and ".generativeAppear(0)"
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Root composes both primitives</done>
</task>

<task type="auto">
  <name>Task 4: Boot the app on an iOS 18 simulator</name>
  <files></files>
  <action>
Build + install + launch on a booted iOS 18 simulator so the next checkpoint can be eyeballed:
`xcrun simctl boot "iPhone 16" || true`; build with `-destination 'platform=iOS Simulator,name=iPhone 16'`;
`xcrun simctl install booted` the .app from DerivedData; `xcrun simctl launch booted com.niko.edge.app`.
If "iPhone 16" is unavailable, pick any available iOS 18 device from `xcrun simctl list devices available`.
  </action>
  <verify>xcrun simctl list devices | grep -i booted</verify>
  <done>App is running on a simulator</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>EDGE foundation — light app showing the breathing iridescent gradient with "EDGE" materializing in. Running on the iOS 18 simulator (or open EDGE/EDGE.xcodeproj and press Cmd-R).</what-built>
  <how-to-verify>Watch the simulator for ~5 seconds and confirm: (1) background is near-white with a soft pastel (blue/lilac/blush/peach/mint) glow that **slowly drifts and breathes** — not static; (2) on launch, the "EDGE" text **fades up from a soft blur into focus** (not an instant pop); (3) the overall feel is light and opalescent, never dark. Then toggle Settings → Accessibility → Motion → Reduce Motion ON, relaunch, and confirm the gradient is static and text appears without blur.</how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (e.g. "gradient too fast", "no blur on entrance")</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] Gradient visibly breathes; entrance blurs into focus (human-verified)
- [ ] Reduce Motion pauses gradient + removes entrance animation (human-verified)
</verification>

<success_criteria>
- All tasks completed
- The two signature primitives work and are wired into the root (FND-03, FND-04)
- User approved the look/motion at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
