---
phase: 04-shell-table
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - EDGE/Sources/Features/Table/TableView.swift
  - EDGE/Sources/Features/Shell/RootView.swift
autonomous: false
requirements: [TABLE-01]
user_setup: []
must_haves:
  truths:
    - "The Table tab lists all 9 players ranked from the feed"
    - "The user's row is visually highlighted; the leader has a crown"
    - "Each row shows a movement arrow with its delta"
    - "Rows assemble with the generative stagger and replay on refresh"
  artifacts:
    - path: "EDGE/Sources/Features/Table/TableView.swift"
      provides: "league standings screen"
      contains: "MovementArrow"
  key_links:
    - from: "EDGE/Sources/Features/Table/TableView.swift"
      to: "AppStore.feed.table"
      via: "renders store.feed?.table"
      pattern: "feed.*table|table"
    - from: "EDGE/Sources/Features/Shell/RootView.swift"
      to: "TableView"
      via: "table tab hosts TableView"
      pattern: "TableView\\(\\)"
---

<objective>
Build the League Table screen and wire it into the Table tab — the first complete real screen.

Purpose: Prove the data + components + shell together on a dense, glanceable standings view (TABLE-01).
Output: TableView.swift (rows, highlight, crown, movement, gap-rail header), wired into RootView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
@EDGE/Sources/Core/Models.swift
@EDGE/Sources/DesignSystem/Components/MovementArrow.swift
@EDGE/Sources/DesignSystem/Components/GapRail.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: TableView (standings)</name>
  <files>EDGE/Sources/Features/Table/TableView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift, EDGE/Sources/DesignSystem/Components/MovementArrow.swift</read_first>
  <action>
Create `TableView.swift` per §7.5. Structure: `@EnvironmentObject var store: AppStore`; body is a
`ScrollView` over `ZStack`-free content (the shared gradient is in RootView). Show a header: title
"League" (`.titleX`) + subtitle "\(feed.me.playersCount) players · \(feed.me.tiebreaker) decides ties"
(`.callout`, `Theme.textDim`). Then a header `GapRail(mine: feed.me.points, leader: feed.me.leaderPoints,
leaderName: feed.me.leaderName)` to visualize the spread. Then a single `FrostCard` containing a
`VStack` of rows over `feed.table` (already rank-sorted). Each row:
- rank `#\(row.rank)` (`.headlineX`, `.monospacedDigit()`; `Theme.actText` if rank==1 else `Theme.textDim`)
- if `row.isLeader`, a `crown.fill` in `Theme.actText`
- `row.name` (`.headlineX`; `Theme.ink` if `row.isMe` else `Theme.text`)
- if `row.isMe`, a `SoftPill(text:"you")`
- spacer, `row.points` (`.headlineX`, `.monospacedDigit()`), `MovementArrow(move: row.move)`
The user's row gets a 3pt iridescent left accent bar + `Theme.actBG.opacity(0.5)` background tint.
Wrap each row in `.generativeAppear(index)` (index increments down the list) and key the content
`.id(store.generation)` so it replays on refresh. Add `.refreshable { await store.refresh() }`.
Guard for `store.feed == nil` with a simple loading placeholder.
NOTE: tapping a row to open a friend's ScoutCard sheet is added in Phase 7 (07-02) — leave rows
non-interactive for now.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TableView.swift contains "feed.me.playersCount", "crown.fill", "row.isMe", "MovementArrow(move: row.move)"
    - contains ".generativeAppear(" and ".id(store.generation)" and ".refreshable"
    - user row uses Theme.actBG (highlight) and Theme.ink (name)
  </acceptance_criteria>
  <done>TableView renders the 9-row standings with highlight/crown/movement</done>
</task>

<task type="auto">
  <name>Task 2: Wire TableView into the Table tab</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>EDGE/Sources/Features/Shell/RootView.swift</read_first>
  <action>
In RootView, replace the `.table` placeholder destination with `TableView()` (inside its
`NavigationStack`, `.navigationBarTitleDisplayMode(.inline)`). Leave Today/Matches/Insights as
placeholders for now.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "TableView()"
  </acceptance_criteria>
  <done>Table tab shows the real standings</done>
</task>

<task type="auto">
  <name>Task 3: Launch + open the Table tab on simulator</name>
  <files></files>
  <action>Build/install/launch on a booted iOS 18 simulator (as in 01-02 Task 4); tap the Table tab.</action>
  <verify>xcrun simctl list devices | grep -i booted</verify>
  <done>Table visible on simulator</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App shell + League Table — four-tab frosted bar; Table tab shows the 9-player standings.</what-built>
  <how-to-verify>Confirm: (1) tapping each of the 4 tabs switches screens with a light haptic and the floating frosted bar highlights the active tab in black; (2) the gradient is vivid on Today and faint on the others; (3) the Table lists Alex(1) → Katha(9), with **Niko's row highlighted** (soft periwinkle tint + iridescent left bar + "you" pill) and a **crown on Alex**; (4) movement arrows show green-up/red-down/gray-dash with numbers; (5) the rows **assemble with the blur-to-focus stagger**, and pull-to-refresh replays it.</how-to-verify>
  <resume-signal>Type "approved" or describe what's off</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] Table shows 9 ranked rows, user highlighted, leader crowned, movement arrows (human-verified)
- [ ] Rows stagger in; refresh replays (human-verified)
</verification>

<success_criteria>
- All tasks completed
- League Table complete and wired into the shell (TABLE-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-shell-table/04-02-SUMMARY.md`
</output>
