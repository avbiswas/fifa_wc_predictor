---
phase: 04-shell-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/Features/Shell/RootView.swift
  - EDGE/Sources/EDGEApp.swift
autonomous: true
requirements: [NAV-01]
user_setup: []
must_haves:
  truths:
    - "Four tabs switch via a custom floating frosted tab bar"
    - "Switching tabs fires a light haptic"
    - "The breathing gradient sits behind all tabs (strong on Today, faint elsewhere)"
  artifacts:
    - path: "EDGE/Sources/Features/Shell/RootView.swift"
      provides: "4-tab shell + floating frosted tab bar + shared gradient + store.feed accessor"
      contains: "toolbar(.hidden, for: .tabBar)"
  key_links:
    - from: "EDGE/Sources/Features/Shell/RootView.swift"
      to: "AppStore"
      via: "@EnvironmentObject store"
      pattern: "@EnvironmentObject"
---

<objective>
Build the 4-tab app shell with the custom floating frosted tab bar and the shared breathing gradient.

Purpose: The navigation frame every screen lives in (NAV-01). Placeholder tab bodies now; real
screens land in later phases.
Output: RootView.swift (shell + Tab enum + custom bar + `store.feed` convenience), EDGEApp root
switched to RootView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
@EDGE/Sources/Core/AppStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab enum + store.feed convenience + shell scaffold</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/AppStore.swift, EDGE/Sources/Core/Models.swift</read_first>
  <action>
In `RootView.swift` add a convenience used by all screens:
`extension AppStore { var feed: Feed? { if case .loaded(let f) = phase { return f }; return nil } }`.
Define `enum Tab: String, CaseIterable { case today, matches, table, insights }` with `title`
("Today"/"Matches"/"Table"/"Insights") and `icon` (`sparkles`/`soccerball`/`trophy`/`chart.bar`)
computed props. Create `struct RootView: View` with `@EnvironmentObject var store: AppStore`,
`@State private var tab: Tab = .today`, and a `ZStack` whose base is
`Theme.bg.ignoresSafeArea()` + `IridescentGlow(intensity: tab == .today ? 1.0 : 0.35).ignoresSafeArea()`,
then a `TabView(selection: $tab)` (each tab a `NavigationStack { PlaceholderTab(tab) }` tagged with
its `Tab`) with `.toolbar(.hidden, for: .tabBar)`. Add a private `PlaceholderTab` that shows the
tab title in a `FrostCard` ("<Title> — coming soon") so the shell runs before real screens exist.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "enum Tab", "toolbar(.hidden, for: .tabBar)", "IridescentGlow(intensity: tab == .today ? 1.0 : 0.35"
    - RootView.swift contains "var feed: Feed?" and "@EnvironmentObject var store: AppStore"
  </acceptance_criteria>
  <done>Shell scaffold compiles with shared gradient</done>
</task>

<task type="auto">
  <name>Task 2: Floating frosted tab bar + haptic</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Shell/RootView.swift</read_first>
  <action>
Add the custom bar per §7.1 via `.safeAreaInset(edge: .bottom)` on the TabView: a centered
`HStack` of the four `Tab.allCases` inside a frosted `Capsule` (`.ultraThinMaterial` + `Theme.hairline`
border + soft `Theme.shadow.opacity(0.10)` shadow), ~56pt tall, ~12pt bottom padding. Each item is a
`Button` that sets `tab` and fires `UIImpactFeedbackGenerator(style: .light).impactOccurred()`. The
SELECTED item shows the SF Symbol in `Theme.ink` plus its title (`.eyebrowX`, `Theme.ink`); unselected
shows only the symbol in `Theme.textMute`. Animate the selection change with a gentle spring.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "safeAreaInset(edge: .bottom)", "UIImpactFeedbackGenerator(style: .light)", "ultraThinMaterial"
    - selected items use Theme.ink; unselected use Theme.textMute
  </acceptance_criteria>
  <done>Floating frosted bar switches tabs with a haptic</done>
</task>

<task type="auto">
  <name>Task 3: Make RootView the app root</name>
  <files>EDGE/Sources/EDGEApp.swift</files>
  <read_first>EDGE/Sources/EDGEApp.swift</read_first>
  <action>
Set the `WindowGroup` content to `RootView().environmentObject(store).preferredColorScheme(.light)
.task { await store.load() }`. Remove the ComponentGallery root (keep the file for reference; it's
still reachable via #Preview).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - EDGEApp.swift contains "RootView()" and ".environmentObject(store)"
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>App launches into the 4-tab shell</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms hidden system bar, custom frosted bar, haptic, per-tab gradient intensity
</verification>

<success_criteria>
- All tasks completed
- 4-tab shell with floating frosted bar + shared breathing gradient (NAV-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-shell-table/04-01-SUMMARY.md`
</output>
