---
phase: 02-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/Core/Models.swift
  - EDGE/Sources/Core/AppStore.swift
  - EDGE/Sources/Core/Format.swift
  - EDGE/Sources/Resources/SampleFeed.json
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup: []
must_haves:
  truths:
    - "App decodes the bundled SampleFeed.json offline into typed models"
    - "AppStore.generation increments when refresh() runs"
    - "Format turns tier/leverage/outcome/strength into words+colors and exact-chance into '1 in N'"
  artifacts:
    - path: "EDGE/Sources/Core/Models.swift"
      provides: "Codable feed models"
      contains: "struct Feed"
    - path: "EDGE/Sources/Core/AppStore.swift"
      provides: "observable store + generation counter + bundled loader"
      contains: "generation"
    - path: "EDGE/Sources/Core/Format.swift"
      provides: "human-language + color mapping helpers"
      contains: "tierStyle"
    - path: "EDGE/Sources/Resources/SampleFeed.json"
      provides: "bundled offline data"
      contains: "\"name\": \"Niko\""
  key_links:
    - from: "EDGE/Sources/Core/AppStore.swift"
      to: "EDGE/Sources/Resources/SampleFeed.json"
      via: "Bundle.main.url(forResource: SampleFeed)"
      pattern: "forResource: \"SampleFeed\""
---

<objective>
Build the offline data layer: Codable models, the observable AppStore with a refresh-driven
generation counter, the bundled sample feed, and the human-language Format helpers.

Purpose: Give every screen real, typed data with zero network and the translation layer that
enforces the no-jargon rule (DATA-01, DATA-02, DATA-03).
Output: Models.swift, AppStore.swift, Format.swift, Resources/SampleFeed.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codable models</name>
  <files>EDGE/Sources/Core/Models.swift</files>
  <read_first>docs/ios_app_plan.md</read_first>
  <action>
Create `Models.swift` with the EXACT structs from `docs/ios_app_plan.md` §4.2: `Feed, Me, Strategy
(+DrawPlan), Form, TableRow, Match (+Team, ScoreResult, Odds, Upset, FieldExposure, Alternate),
Pick (+PickResult), ScoutReport (+Trait)`. Keep `Identifiable` conformances and the exact field
names — they must match the JSON keys in §4.1/§18 verbatim (e.g. `exactChanceOneIn`, `favoriteStrength`,
`isLeader`). Use `let` properties and the nested-struct layout shown.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - Models.swift contains "struct Feed: Codable", "struct Match: Codable, Identifiable", "struct Pick: Codable"
    - contains "exactChanceOneIn" and "favoriteStrength" and "struct Trait"
  </acceptance_criteria>
  <done>All models compile</done>
</task>

<task type="auto">
  <name>Task 2: Bundled SampleFeed.json</name>
  <files>EDGE/Sources/Resources/SampleFeed.json</files>
  <read_first>docs/ios_app_plan.md</read_first>
  <action>
Create `EDGE/Sources/Resources/SampleFeed.json` with the EXACT JSON from `docs/ios_app_plan.md` §18
(the full appendix sample — `me`, `strategy`, `form`, the 9-row `table`, the 4 `matches` including
the `final` France 2:1 Senegal and the `chaos` Ghana 1:1 Panama, and the 8 `scouting` cards). Copy
it verbatim, including the German names with umlauts (Schirifötze) and the `…` in "Klose4ev…". Do
NOT abbreviate or trim entries. xcodegen bundles everything under `Sources/`, so this file ships as
a bundle resource reachable via `Bundle.main.url(forResource:"SampleFeed", withExtension:"json")`.
  </action>
  <verify>python3 -m json.tool EDGE/Sources/Resources/SampleFeed.json > /dev/null && echo "VALID JSON"</verify>
  <acceptance_criteria>
    - `python3 -m json.tool` parses the file with no error (valid JSON)
    - file contains '"name": "Niko"', '"rank": 8', '"leaderName": "Alex"', '"status":"final"', '"tier":"chaos"'
    - file contains 8 scouting entries (grep -c '"traits"' returns 8)
  </acceptance_criteria>
  <done>Valid, complete sample feed bundled</done>
</task>

<task type="auto">
  <name>Task 3: AppStore (observable + generation + bundled loader)</name>
  <files>EDGE/Sources/Core/AppStore.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift</read_first>
  <action>
Create `AppStore.swift` with the EXACT `@MainActor final class AppStore: ObservableObject` from §4.3:
`Phase` enum (loading/loaded/failed), `@Published phase`, `@Published generation`, the `iso8601`
decoder, `load()` (loads bundled first, bumps `generation`, then optional remote), `refresh()`, and
`static loadBundled()`. Keep `feedURL = nil` (offline v1). `generation` MUST increment on every load
so screens can replay their assembly. Also update `EDGEApp.swift` to inject the store:
`@StateObject private var store = AppStore()` … `.environmentObject(store)` … `.task { await store.load() }`
(keep the Phase-1 placeholder body for now; Phase 4 swaps in RootView).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - AppStore.swift contains "ObservableObject", "@Published var generation", "forResource: \"SampleFeed\"", "func refresh()"
    - AppStore.swift increments generation in load() (contains "generation += 1")
    - EDGEApp.swift contains "@StateObject private var store" and ".environmentObject(store)" and ".task { await store.load() }"
  </acceptance_criteria>
  <done>Store loads bundled feed offline and bumps generation on refresh</done>
</task>

<task type="auto">
  <name>Task 4: Format helpers (human-language + colors)</name>
  <files>EDGE/Sources/Core/Format.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `Format.swift` with the EXACT `enum Format` from §6: `tierStyle`, `tierRingColor`,
`strengthWords`, `leverageChip`, `leaderChip`, `ringFill(oneIn:)`, `chanceText(oneIn:)`,
`outcomeChip`, `modeIcon`, `greeting`, `countdown`, `kickoffClock`, `relativeUpdated`. These return
SwiftUI `Color` from Theme tokens (soft state pairs) and human strings only. This file is the
enforcement point for LANG-01 (no raw %, no jargon) — it must never produce a probability string.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3; python3 -m json.tool EDGE/Sources/Resources/SampleFeed.json > /dev/null && echo JSON_OK</verify>
  <acceptance_criteria>
    - Format.swift contains "func tierStyle", "func chanceText", 'return "1 in \\(max(oneIn,1))"', "func outcomeChip"
    - Format.swift references Theme.posText / Theme.warnText / Theme.actBG
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Translation layer compiles and only emits words/colors</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m json.tool` validates SampleFeed.json
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms model field names match JSON keys and AppStore bumps generation
</verification>

<success_criteria>
- All tasks completed
- Bundled feed decodes offline; store exposes feed + generation (DATA-01, DATA-02)
- Format converts everything to words/colors (DATA-03), upholding LANG-01
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-01-SUMMARY.md`
</output>
