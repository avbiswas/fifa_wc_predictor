---
phase: 03-components
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - EDGE/Sources/DesignSystem/Components/GeneratedStatus.swift
  - EDGE/Sources/DesignSystem/Components/Sparkline.swift
  - EDGE/Sources/Features/Shell/ComponentGallery.swift
autonomous: false
requirements: [COMP-03]
user_setup: []
must_haves:
  truths:
    - "GeneratedStatus shows a pulsing spark dot with a generating/generated label"
    - "Sparkline renders recent points as tiny bars (zero = muted)"
    - "A gallery screen shows every component rendering correctly on the light canvas"
  artifacts:
    - path: "EDGE/Sources/DesignSystem/Components/GeneratedStatus.swift"
      provides: "agentic status affordance"
      contains: "Generating your briefing"
    - path: "EDGE/Sources/DesignSystem/Components/Sparkline.swift"
      provides: "tiny points bars"
      contains: "struct Sparkline"
    - path: "EDGE/Sources/Features/Shell/ComponentGallery.swift"
      provides: "visual proof gallery of all components"
      contains: "ComponentGallery"
---

<objective>
Build GeneratedStatus + Sparkline, then a ComponentGallery that renders every component for visual
verification.

Purpose: Complete the component vocabulary (COMP-03) and prove the whole library looks right before
screens are assembled. The gallery is the design-fidelity gate for Phase 3.
Output: GeneratedStatus.swift, Sparkline.swift, ComponentGallery.swift; a verified gallery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/DesignSystem/Theme.swift
@EDGE/Sources/Core/Format.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: GeneratedStatus</name>
  <files>EDGE/Sources/DesignSystem/Components/GeneratedStatus.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Format.swift</read_first>
  <action>
Create `GeneratedStatus.swift` with the EXACT `struct GeneratedStatus` from §5.13: a `Theme.spark`
dot that pulses (scale 0.8↔1.2, opacity 0.5↔1) via `.repeatForever`, next to text that is
"Generating your briefing…" when `generating`, else "Generated \(Format.relativeUpdated(updatedAt))".
Inputs: `updatedAt: Date`, `generating: Bool = false`.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - GeneratedStatus.swift contains "Generating your briefing", "Theme.spark", "repeatForever"
  </acceptance_criteria>
  <done>Status affordance compiles</done>
</task>

<task type="auto">
  <name>Task 2: Sparkline</name>
  <files>EDGE/Sources/DesignSystem/Components/Sparkline.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Theme.swift</read_first>
  <action>
Create `Sparkline.swift` with the EXACT `struct Sparkline` from §5.14: a bottom-aligned HStack of
rounded bars sized by `values[i] / max`, colored `Theme.iridLilac` (or `Theme.track` when the value
is 0). Height 34.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - Sparkline.swift contains "struct Sparkline" and "Theme.iridLilac" and a values array input
  </acceptance_criteria>
  <done>Sparkline compiles</done>
</task>

<task type="auto">
  <name>Task 3: ComponentGallery (visual proof) + temporary root swap</name>
  <files>EDGE/Sources/Features/Shell/ComponentGallery.swift, EDGE/Sources/EDGEApp.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/DesignSystem/Components/FrostCard.swift, EDGE/Sources/DesignSystem/Components/SoftRing.swift</read_first>
  <action>
Create `ComponentGallery.swift` — a `ScrollView` over `ZStack { Theme.bg; IridescentGlow(intensity:0.5) }`
that shows, each inside a `FrostCard` with a short `Eyebrow` label:
- `SoftRing(value: 1.0/7.0, color: Theme.warnText){ Text("1:0").font(.displayX) }`
- `StrengthBar(home:0.74, draw:0.20, away:0.06, homeCode:"POR", awayCode:"COD")`
- `GapRail(mine:18, leader:24, leaderName:"Alex")`
- a row of `SoftPill` in all four styles (mint/peach/rose/periwinkle) using Format outputs
- an `InkButton(title:"Why this pick"){}`
- `MovementArrow(move:3)`, `MovementArrow(move:-2)`, `MovementArrow(move:0)`
- `TeamBadge(code:"BRA")`
- `GeneratedStatus(updatedAt: .now)`
- `Sparkline(values:[2,0,4,2,0,3,2,0,4])`
Temporarily set `EDGEApp`'s root to `ComponentGallery()` (Phase 4 swaps in `RootView`).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - ComponentGallery.swift references SoftRing, StrengthBar, GapRail, SoftPill, InkButton, MovementArrow, TeamBadge, GeneratedStatus, Sparkline
    - EDGEApp.swift root is ComponentGallery() (contains "ComponentGallery(")
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Gallery builds and shows every component</done>
</task>

<task type="auto">
  <name>Task 4: Launch gallery on simulator</name>
  <files></files>
  <action>Build/install/launch on a booted iOS 18 simulator (as in 01-02 Task 4) so the gallery can be eyeballed.</action>
  <verify>xcrun simctl list devices | grep -i booted</verify>
  <done>Gallery running on simulator</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Component gallery — every EDGE component on one scrollable light screen, over a faint breathing gradient.</what-built>
  <how-to-verify>Scroll the gallery and confirm: (1) cards are clearly **frosted glass** (translucent, soft white edge, gentle shadow) on the light canvas; (2) the ring's arc **animates from empty to ~1/7 full** on appear and shows "1:0" centered; (3) the strength bar fills and the gap rail's marker slides in; (4) the four soft pills read as gentle pastels with darker text (mint/peach/rose/periwinkle), not neon; (5) the InkButton is a clean black pill; (6) movement arrows are green-up / red-down / gray-dash. Nothing should look harsh, dark, or default-iOS.</how-to-verify>
  <resume-signal>Type "approved" or list any component that looks off</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] Gallery visually confirms frosted cards + animating ring/bar/rail + pastel pills (human-verified)
</verification>

<success_criteria>
- All tasks completed
- GeneratedStatus/Sparkline render; full component library visually approved (COMP-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-components/03-03-SUMMARY.md`
</output>
