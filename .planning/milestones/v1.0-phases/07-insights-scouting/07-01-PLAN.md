---
phase: 07-insights-scouting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/Features/Insights/InsightsView.swift
  - EDGE/Sources/Features/Shell/RootView.swift
autonomous: true
requirements: [INSIGHT-01]
user_setup: []
must_haves:
  truths:
    - "Insights shows engine form: points won, avg per game, spot-on count, scored ratio"
    - "A sparkline shows recent points per pick"
  artifacts:
    - path: "EDGE/Sources/Features/Insights/InsightsView.swift"
      provides: "engine-form metrics + sparkline"
      contains: "Sparkline"
  key_links:
    - from: "EDGE/Sources/Features/Insights/InsightsView.swift"
      to: "AppStore.feed.form"
      via: "renders form metrics"
      pattern: "feed.form"
---

<objective>
Build the Insights tab's engine-form section: soft metric tiles + a sparkline + a human takeaway.

Purpose: Show how good the picks have been, in human framing (INSIGHT-01, part A). Scouting list is
added in 07-02.
Output: InsightsView.swift (form section) wired into the Insights tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/Core/Models.swift
@EDGE/Sources/DesignSystem/Components/Sparkline.swift
@EDGE/Sources/DesignSystem/Components/FrostCard.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: InsightsView — form metric tiles + sparkline</name>
  <files>EDGE/Sources/Features/Insights/InsightsView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift, EDGE/Sources/DesignSystem/Components/Sparkline.swift</read_first>
  <action>
Create `InsightsView` per §7.6 (part A). `@EnvironmentObject var store`; `ScrollView` of a
left-aligned VStack; guard `store.feed`. Define a private `MetricTile(value: String, caption: String,
accent: Bool)` = a frosted tile (`RoundedRectangle rMd` fill `Theme.card` + `Theme.hairline` stroke)
with `value` in `.displayX` (`Theme.posText` if accent else `Theme.text`, `.monospacedDigit()`) over an
`Eyebrow(caption)`. Content:
- `Eyebrow("How the picks are doing · last \(feed.form.matches) games")`
- a `LazyVGrid(columns: 2)` of tiles: `MetricTile("\(feed.form.points)", "points won", accent: true)`,
  `MetricTile(String(format: "%.1f", feed.form.avgPoints), "avg per game", false)`,
  `MetricTile("\(feed.form.exact)", "spot-on scores", accent: true)`,
  `MetricTile("\(feed.form.scored)/\(feed.form.matches)", "games that scored", false)`
- a `FrostCard` with `Sparkline(values: feed.form.recentPoints)` + caption "points per pick, most
  recent on the right" (`.callout`, dim)
- a takeaway line (`.body`): e.g. "Scored in \(feed.form.scored) of \(feed.form.matches) and nailed
  \(feed.form.exact) exact — steady." (compose from the numbers).
Each block `.generativeAppear(i)`; wrap `.id(store.generation)`; add `.refreshable { await store.refresh() }`.
(`feed.form.avgPoints` is the ONLY decimal shown, and it's points-per-game, not a probability — allowed.)
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - InsightsView.swift contains "feed.form.points", "feed.form.exact", "Sparkline(values: feed.form.recentPoints)"
    - contains a 2-column grid of metric tiles and a takeaway line
    - contains ".generativeAppear(", ".id(store.generation)", ".refreshable"
  </acceptance_criteria>
  <done>Insights form section renders</done>
</task>

<task type="auto">
  <name>Task 2: Wire InsightsView into the Insights tab</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>EDGE/Sources/Features/Shell/RootView.swift</read_first>
  <action>Replace the `.insights` placeholder with `InsightsView()` inside its NavigationStack.</action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "InsightsView()" — all four tabs now host real screens
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Insights tab shows the form section</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms form metrics + sparkline + takeaway
</verification>

<success_criteria>
- All tasks completed
- Insights form section renders (INSIGHT-01 part A; scouting in 07-02)
</success_criteria>

<output>
After completion, create `.planning/phases/07-insights-scouting/07-01-SUMMARY.md`
</output>
