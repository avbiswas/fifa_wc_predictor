---
phase: 07-insights-scouting
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - EDGE/Sources/Features/Insights/ScoutCard.swift
  - EDGE/Sources/Features/Insights/InsightsView.swift
  - EDGE/Sources/Features/Table/TableView.swift
autonomous: false
requirements: [INSIGHT-01]
user_setup: []
must_haves:
  truths:
    - "A scouting card per friend shows tag, blurb, and 3 trait level bars"
    - "The Insights tab lists scouting cards sorted by rank"
    - "Tapping a non-you table row opens that friend's scouting card"
  artifacts:
    - path: "EDGE/Sources/Features/Insights/ScoutCard.swift"
      provides: "friend scouting card"
      contains: "struct ScoutCard"
  key_links:
    - from: "EDGE/Sources/Features/Insights/InsightsView.swift"
      to: "ScoutCard"
      via: "lists scouting"
      pattern: "ScoutCard("
    - from: "EDGE/Sources/Features/Table/TableView.swift"
      to: "ScoutCard"
      via: "row tap presents a ScoutCard sheet"
      pattern: "sheet"
---

<objective>
Build the friend ScoutCard, list it in Insights, and wire the Table rows to open a friend's scouting
card.

Purpose: Turn each opponent's tendencies into a glanceable scouting report (INSIGHT-01, part B) and
connect it from the standings.
Output: ScoutCard.swift, scouting list in InsightsView, Table-row → ScoutCard sheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/Core/Models.swift
@EDGE/Sources/DesignSystem/Components/FrostCard.swift
@EDGE/Sources/DesignSystem/Components/SoftPill.swift
@EDGE/Sources/Features/Table/TableView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoutCard</name>
  <files>EDGE/Sources/Features/Insights/ScoutCard.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift</read_first>
  <action>
Create `struct ScoutCard: View { let report: ScoutReport }` per §7.7, in a `FrostCard`:
- header: optional `crown.fill` (`Theme.actText`) if `report.isLeader`, `report.name` (`.headlineX`),
  trailing `"#\(report.rank) · \(report.points) pts"` (`.callout`, dim)
- a `SoftPill(text: report.tag)` whose color is chosen by keyword: tag lowercased contains "chaos" →
  (warnText/warnBG); contains "contrarian"/"away" → (actText/actBG); else neutral
  (textDim / `Color(hex:"F0F0F3")`)
- `report.blurb` (`.bodyX`, `Theme.text`)
- a divider, then for each `report.traits`: an HStack of `Eyebrow(trait.label)` (fixed width ~70),
  a 3-segment level bar (3 capsules; first `trait.level` filled `Theme.iridLilac`, rest `Theme.track`),
  and `trait.value` (`.callout`).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - ScoutCard.swift contains "report.tag", "report.blurb", "report.traits", a 3-segment level bar using trait.level and Theme.iridLilac
    - tag color switches on "chaos"/"contrarian"
  </acceptance_criteria>
  <done>ScoutCard compiles</done>
</task>

<task type="auto">
  <name>Task 2: Scouting list in InsightsView</name>
  <files>EDGE/Sources/Features/Insights/InsightsView.swift</files>
  <read_first>EDGE/Sources/Features/Insights/InsightsView.swift, EDGE/Sources/Features/Insights/ScoutCard.swift</read_first>
  <action>
Append below the form section: `Eyebrow("Scouting the field")` then a `ForEach(feed.scouting.sorted
{ $0.rank < $1.rank })` of `ScoutCard(report:)`, each `.generativeAppear(i)` (continue the index).
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - InsightsView.swift contains 'Eyebrow("Scouting the field")' and "ScoutCard(report:" and "feed.scouting.sorted"
  </acceptance_criteria>
  <done>Insights lists a scouting card per friend</done>
</task>

<task type="auto">
  <name>Task 3: Table row → ScoutCard sheet</name>
  <files>EDGE/Sources/Features/Table/TableView.swift</files>
  <read_first>EDGE/Sources/Features/Table/TableView.swift, EDGE/Sources/Features/Insights/ScoutCard.swift</read_first>
  <action>
Make each NON-you table row tappable (`Button`/`onTapGesture`) to set `@State private var
scouted: ScoutReport?`, matching the row to `feed.scouting.first { $0.name == row.name }` (skip if
none — e.g. the user). Present `.sheet(item: $scouted) { ScoutCard(report: $0).padding()
.presentationDetents([.medium]) }` over a `Theme.bg` background. (Requires `ScoutReport: Identifiable`
— it already is via `id { name }`.)
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - TableView.swift contains ".sheet(item:", "ScoutCard(report:", "feed.scouting.first"
  </acceptance_criteria>
  <done>Tapping a friend's row opens their scouting card</done>
</task>

<task type="auto">
  <name>Task 4: Launch + open Insights and a scouting sheet</name>
  <files></files>
  <action>Build/install/launch on a booted iOS 18 simulator; open Insights, then tap UnsPascha's row in the Table.</action>
  <verify>xcrun simctl list devices | grep -i booted</verify>
  <done>Scouting visible from both Insights and Table</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Insights tab (engine form + scouting) and Table-row → scouting sheet.</what-built>
  <how-to-verify>In Insights confirm: four metric tiles (24 points won / 1.5 avg / 4 spot-on / 8/16 scored), a sparkline, a takeaway line, then a **scouting card per friend** — each with a tag pill (UnsPascha = "Chaos merchant" in peach), a blurb, and **3 trait level bars** (Goals/Draws/Upsets filled to level). In the Table, tap UnsPascha's row → their scouting card slides up as a sheet. Everything stays light/frosted; cards assemble with the stagger.</how-to-verify>
  <resume-signal>Type "approved" or list anything off</resume-signal>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] Insights shows form + a scouting card per friend; Table row opens a scouting sheet (human-verified)
</verification>

<success_criteria>
- All tasks completed
- Insights + scouting complete; standings link to scouting (INSIGHT-01)
</success_criteria>

<output>
After completion, create `.planning/phases/07-insights-scouting/07-02-SUMMARY.md`
</output>
