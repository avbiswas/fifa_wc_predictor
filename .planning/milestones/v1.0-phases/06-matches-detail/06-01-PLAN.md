---
phase: 06-matches-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EDGE/Sources/Features/Matches/MatchRowCard.swift
  - EDGE/Sources/Features/Matches/MatchesView.swift
  - EDGE/Sources/Features/Shell/RootView.swift
autonomous: true
requirements: [MATCH-01]
user_setup: []
must_haves:
  truths:
    - "Matches shows Upcoming and Results via a frosted segmented control, grouped by matchday"
    - "Upcoming rows show countdown, tier pill, teams, pick ring, strength bar"
    - "Final rows show the actual score + a soft outcome pill (e.g. Spot on +4 / Missed)"
  artifacts:
    - path: "EDGE/Sources/Features/Matches/MatchRowCard.swift"
      provides: "upcoming + final match card"
      contains: "Format.outcomeChip"
    - path: "EDGE/Sources/Features/Matches/MatchesView.swift"
      provides: "segmented Upcoming/Results list"
      contains: "navigationDestination(for: String"
  key_links:
    - from: "EDGE/Sources/Features/Matches/MatchesView.swift"
      to: "MatchDetailView"
      via: "navigationDestination resolves match.id"
      pattern: "MatchDetailView(match:"
---

<objective>
Build the Matches tab: a segmented Upcoming/Results list of match cards, wired into the shell and to
match detail.

Purpose: Scores + upcoming games in one glanceable list (MATCH-01).
Output: MatchRowCard.swift, MatchesView.swift, Matches tab wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ios_app_plan.md
@EDGE/Sources/Core/Models.swift
@EDGE/Sources/Core/Format.swift
@EDGE/Sources/DesignSystem/Components/SoftRing.swift
@EDGE/Sources/DesignSystem/Components/StrengthBar.swift
@EDGE/Sources/DesignSystem/Components/CountdownPill.swift
@EDGE/Sources/DesignSystem/Components/SoftPill.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchRowCard (upcoming + final variants)</name>
  <files>EDGE/Sources/Features/Matches/MatchRowCard.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Core/Models.swift, EDGE/Sources/Core/Format.swift</read_first>
  <action>
Create `struct MatchRowCard: View { let match: Match }` per §7.3, rendered inside a `FrostCard`,
branching on `match.status`:
- **upcoming**: top row `CountdownPill(kickoff: match.kickoff)` + trailing `SoftPill` tier (via
  `Format.tierStyle(match.myPick.tier, label: match.myPick.tierLabel)`); team rows
  `TeamBadge(home.code)`+name and away, with a trailing
  `SoftRing(value: Format.ringFill(oneIn: match.myPick.exactChanceOneIn),
  color: Format.tierRingColor(match.myPick.tier)){ Text(match.myPick.score).font(.displayX) }`;
  then `StrengthBar(...)`.
- **final**: top row `Eyebrow("Matchday \(match.matchday)")` + trailing `SoftPill` from
  `Format.outcomeChip(match.myPick.result!.outcome, points: match.myPick.result!.points)`; team rows
  showing the **actual score** big (`.displayX`, colored with the outcomeChip `fg`: green-ish if any
  points, rose if miss) next to each team; a small `"your tip \(match.myPick.score)"` with a
  trailing `checkmark`/`xmark` (✓ if outcome != "miss"). Use `match.result!.home`/`.away`.
Whole card is tappable via the parent's NavigationLink.
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MatchRowCard.swift branches on match.status ("upcoming"/"final")
    - contains "CountdownPill(", "SoftRing(", "Format.outcomeChip", "match.result"
    - final variant shows actual score + "your tip" + a checkmark/xmark
  </acceptance_criteria>
  <done>Both card variants compile and read correctly</done>
</task>

<task type="auto">
  <name>Task 2: MatchesView (segmented, grouped) + detail destination</name>
  <files>EDGE/Sources/Features/Matches/MatchesView.swift</files>
  <read_first>docs/ios_app_plan.md, EDGE/Sources/Features/Matches/MatchRowCard.swift, EDGE/Sources/Features/Today/TodayView.swift</read_first>
  <action>
Create `MatchesView` per §7.3: `@EnvironmentObject var store`, `@State private var tab: Seg = .upcoming`
(`enum Seg { case upcoming, results }`). Header: title "Matches" (`.titleX`) + a **frosted segmented
control** = a frosted `Capsule` holding two buttons ("Upcoming" / "Results"); the selected button gets
a white inner `Capsule` + `Theme.ink` text, the other `Theme.textDim`. Filter:
upcoming = `status == "upcoming"` sorted by kickoff; results = `status == "final"` sorted by matchday
desc. Group the chosen set by `matchday` with an `Eyebrow("Matchday \(n)")` per group, then a
`NavigationLink(value: match.id) { MatchRowCard(match:) }` per match. Wrap list `.id(store.generation)`,
add `.refreshable { await store.refresh() }`, each link `.generativeAppear(i)`. Add
`.navigationDestination(for: String.self) { id in if let m = store.feed?.matches.first(where:{ $0.id == id })
{ MatchDetailView(match: m) } }`. Empty states per §10: results empty → "No results yet — first picks
score after kickoff."; upcoming empty → "All caught up."
  </action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - MatchesView.swift contains the segmented control (two states), groups by matchday
    - contains "NavigationLink(value:", "navigationDestination(for: String.self)", "MatchDetailView(match:"
    - contains both empty-state strings
  </acceptance_criteria>
  <done>Segmented, grouped matches list compiles</done>
</task>

<task type="auto">
  <name>Task 3: Wire MatchesView into the Matches tab</name>
  <files>EDGE/Sources/Features/Shell/RootView.swift</files>
  <read_first>EDGE/Sources/Features/Shell/RootView.swift</read_first>
  <action>Replace the `.matches` placeholder with `MatchesView()` inside its NavigationStack.</action>
  <verify>cd EDGE && xcodegen generate && xcodebuild -project EDGE.xcodeproj -scheme EDGE -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - RootView.swift contains "MatchesView()"
    - xcodebuild prints BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Matches tab shows the list</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild BUILD SUCCEEDED
- [ ] grep confirms segmented control, matchday grouping, both card variants, detail destination
</verification>

<success_criteria>
- All tasks completed
- Matches Upcoming/Results list renders with picks + scores (MATCH-01)
</success_criteria>

<output>
After completion, create `.planning/phases/06-matches-detail/06-01-SUMMARY.md`
</output>
